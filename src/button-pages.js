import React from "react";

export const ButtonPages = () => {

    return(
        <div>
            <button onClick={() => console.log("ViewMore")}>
                View more
            </button>
        </div>
    );
}